import { ContactsBlock, Container, Title } from '@/shared/components/shared';

export default async function StructurePage() {
  return (
    <Container>
      <Title text={'Структура ОКТУ'} size="2xl" className={'mt-4 mb-12'} />
    </Container>
  );
}
